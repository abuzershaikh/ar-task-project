import 'package:flutter/material.dart';
import '../../rewards/screens/rewards_screen.dart';

/// Dedicated Day Streak Screen:
/// - Direct wrapper over 3D Gamified Daily Strike & Rewards screen.
class DayStreakScreen extends StatelessWidget {
  const DayStreakScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const RewardsScreen();
  }
}
